    <footer>
        <p>&copy; <%= new Date().getFullYear() %> CSE 340 Service Network.
        <% /* Display (Development mode) only when NODE_ENV is set to development */ %>
        <% if (NODE_ENV === 'development') { %>
            (Development mode)
        <% } %>
        </p>
    </footer>
</body>
</html>